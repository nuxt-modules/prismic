{
  "name": "docs",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "publish": "npm run generate && push-dir --dir dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "@nuxt/content-theme-docs": "^0.3.1",
    "defu": "^3.1.0",
    "nuxt": "^2.14.1"
  },
  "devDependencies": {
    "@nuxtjs/google-analytics": "^2.4.0",
    "push-dir": "0.4.1"
  }
}
